from __future__ import annotations

def classification_metrics(logits, labels) -> dict    # F1, precision, recall, AUC, MRR, Hits@K
def ranking_metrics(scores, labels) -> dict           # későbbi ranking-hez
